<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="memberMapper">
	<resultMap type="Member" id="memberResultSet">
		<!-- MYBATIS에서 id는 pk를 뜻함 -->
		<id property="user_sq" column="user_sq" />
		<!-- <result property="user_sq" column="user_sq" /> -->
		<result property="user_id" column="user_id" />
		<result property="user_pwd" column="user_pwd" />
		<result property="user_name" column="user_name" />
		<result property="email" column="email" />
		<result property="phone" column="phone" />
		<result property="address" column="address" />
		<result property="address" column="ADDRESS" />
	</resultMap>

	<select id="loginMember" parameterType="Member"
		resultMap="memberResultSet">
		<!-- 암호화 전 -->
		<!-- SELECT *
		FROM MEMBER
		WHERE ID=#{id}
		AND PWD=#{pwd}
		AND M_STATUS='Y' -->
		
		<!-- 암호화 후 -->
		SELECT *
		FROM user_tb
		WHERE user_id=#{user_id}
		
	</select>

	<insert id="insertMember" parameterType="Member">
		INSERT INTO user_tb VALUES (user_seq.nextval,#{user_id},#{user_pwd},#{user_name},#{email},#{phone},#{address})
	</insert>
	
	<update id="updateMember" parameterType="Member">
		UPDATE MEMBER
		SET EMAIL = #{email},AGE=#{age},PHONE=#{phone},ADDRESS=#{address},UPDATE_DATE=SYSDATE
		WHERE ID = #{id}
	</update>
	
	<update id="deleteMember" parameterType="string">
		UPDATE MEMBER
		SET M_STATUS='N'
		WHERE ID = #{id}
	</update>
	
	<insert id="insertBoard" parameterType="Member">
		INSERT INTO BOARD(BID,BTITLE)
		VALUES(SEQ_BID.NEXTVAL,'새로운 회원이 가입되었습니다.')
	</insert>
	
	<select id="idCheck" parameterType="string" resultType="_int">
		SELECT COUNT(*)
		FROM MEMBER
		WHERE ID=#{id}
	</select>
	
	<insert id="insertProfile" parameterType="MemberProfile">
		INSERT INTO profile_tb VALUES (profile_seq.nextval,#{user_sq},#{profile_nickname},#{profile_gender},default,default,default)
	</insert>
</mapper>